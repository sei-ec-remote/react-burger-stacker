import React from 'react'

const ClearBurger = ({ clearStack }) => {
        return(
                <button className="clearButton" onClick={clearStack}>Clear</button>
        )
    }



export default ClearBurger